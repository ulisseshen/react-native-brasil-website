<!-- ia-translated: true -->
:::warning
Se você instalou anteriormente um pacote global `react-native-cli`, por favor, remova-o pois ele pode causar problemas inesperados:

```shell
npm uninstall -g react-native-cli @react-native-community/cli
```

:::
